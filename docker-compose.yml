services:
  cpp-builder:
    build:
      context: .
    volumes:
      - .:/app
    working_dir: /app
    command: bash -c "chmod +x build.sh check-dependencies.sh && ./check-dependencies.sh && ./build.sh --verify"
